{
  "name": "@ldesign/git-web-ui",
  "version": "0.5.0",
  "description": "LDesign Git Web UI - 图形化界面",
  "keywords": [
    "git",
    "web-ui",
    "dashboard",
    "visual"
  ],
  "author": "LDesign Team",
  "license": "MIT",
  "type": "module",
  "bin": {
    "ldesign-git-ui": "./bin/web-ui.js"
  },
  "files": [
    "bin",
    "client/dist",
    "server/dist",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev:client\" \"npm run dev:server\"",
    "dev:client": "cd client && npm run dev",
    "dev:server": "cd server && npm run dev",
    "build": "npm run build:client && npm run build:server",
    "build:client": "cd client && npm run build",
    "build:server": "tsup server/src/index.ts --outDir server/dist --format esm --dts --clean --tsconfig server/tsconfig.json",
    "install:all": "cd client && npm install && cd ../server && npm install",
    "clean": "rimraf client/dist server/dist",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "@ldesign/git-core": "workspace:*",
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "ws": "^8.16.0",
    "chokidar": "^3.6.0"
  },
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^4.17.21",
    "@types/node": "^20.11.16",
    "@types/ws": "^8.5.10",
    "concurrently": "^8.2.2",
    "rimraf": "^5.0.5",
    "tsup": "^8.0.0",
    "typescript": "^5.7.3"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}